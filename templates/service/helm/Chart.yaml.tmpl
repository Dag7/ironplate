# =============================================================================
# {{ .Service.Group }} Services Chart
# =============================================================================
# Umbrella chart for {{ .Project.Metadata.Name }} {{ .Service.Group }} services.
# Depends on the shared service library chart for rendering.
# =============================================================================
apiVersion: v2
name: {{ .Service.Group }}-services
description: {{ .Project.Metadata.Name }} {{ .Service.Group }} services
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: {{ .Project.Metadata.Name }}-service
    version: "1.0.0"
    repository: "file://../_lib/service"
